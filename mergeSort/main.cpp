#include <iostream>

using namespace std;

template <typename  T>void merge(T* initList,T* mergeList,const int l, const m, const n)
{

}

template<typename T>void mergePass(T* initList,T* mergeList,const int l, const s)
{
}

template<typename T>void mergeSort(T* initList, const a)
{

}
int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}